export const enum SceneList {
	Start = 'c_start',
	Plan = 'c_plan',
	PickTime = 'c_pick-time',
	Settings = 'c_settings',
	SetName = 'c_set-name',
	ListEvents = 'c_list-events',
	Ban = 'c_ban',
	Invite = 'c_invite',
}
